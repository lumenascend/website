<div class="relative w-full max-w-5xl mx-auto">
    <!-- Slides -->
    <div class="overflow-hidden relative">
      <div class="flex transition-transform duration-500 ease-in-out" id="carousel-slides">
        <!-- Slide 1 -->
        <div class="flex-shrink-0 w-full h-full flex flex-col md:flex-row items-center justify-center">
            <div class="flex flex-col p-8">
                <h2 class="text-xl font-bold text-royalPlum pb-8">Our Values</h2>
                <h3 class="text-6xl font-bold">Purpose</h3>
            </div>
            <div class="flex flex-col md:flex-row p-8 text-4xl relative">
                <span class="text-royalPlum text-9xl absolute top-0 left-[-16px]">&ldquo;</span> 
                    We build software with intent, ensuring every solution drives meaningful results for our clients and their users. Our focus is on solving real problems with efficiency and innovation.
                <span class="text-royalPlum text-9xl absolute bottom-[-64px] right-[-8px]">&rdquo;</span> 

            </div>
        </div>
        <!-- Slide 2 -->
        <div class="flex-shrink-0 w-full h-full flex flex-col md:flex-row items-center justify-center">
            <div class="flex flex-col p-8">
                <h2 class="text-xl font-bold text-royalPlum pb-8">Our Values</h2>
                <h3 class="text-6xl font-bold">Empathy</h3>
            </div>
            <div class="flex flex-col md:flex-row p-8 text-4xl">
                We prioritise understanding the needs of our clients and their customers. By putting people first, we create user-friendly, accessible, and impactful solutions that truly make a difference.
            </div>
        </div>
        <!-- Slide 3 -->
        <div class="flex-shrink-0 w-full h-full flex flex-col md:flex-row items-center justify-center">
            <div class="flex flex-col p-8">
                <h2 class="text-xl font-bold text-royalPlum pb-8">Our Values</h2>
                <h3 class="text-6xl font-bold">Impact</h3>
            </div>
            <div class="flex flex-col md:flex-row p-8 text-4xl">
                Our work goes beyond just delivering software. We aim to create lasting value, helping businesses grow, improve operations, and achieve their goals through technology.
            </div>
        </div>
      </div>
    </div>
  
    <!-- Indicators -->
    <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 flex space-x-2">
      <button class="w-3 h-3 bg-gray-500 rounded-full" data-slide="0"></button>
      <button class="w-3 h-3 bg-gray-500 rounded-full" data-slide="1"></button>
      <button class="w-3 h-3 bg-gray-500 rounded-full" data-slide="2"></button>
    </div>
  </div>
  
  <script>
    const slides = document.getElementById('carousel-slides');
    const indicators = document.querySelectorAll('[data-slide]');
    let currentIndex = 0;
    const slideCount = indicators.length;
    const intervalTime = 5000; // 5 seconds
  
    // Function to update the carousel
    function updateCarousel() {
      slides.style.transform = `translateX(-${currentIndex * 100}%)`;
      indicators.forEach((indicator, index) => {
        indicator.classList.toggle('bg-gray-800', index === currentIndex);
        indicator.classList.toggle('bg-gray-500', index !== currentIndex);
      });
    }
  
    // Autoplay functionality
    function startAutoplay() {
      setInterval(() => {
        currentIndex = (currentIndex + 1) % slideCount;
        updateCarousel();
      }, intervalTime);
    }
  
    // Add click event listeners for indicators
    indicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => {
        currentIndex = index;
        updateCarousel();
      });
    });
  
    // Initialize the carousel
    updateCarousel();
    startAutoplay();
  </script>